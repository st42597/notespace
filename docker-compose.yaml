services:
  client:
    build:
      context: ./client
    container_name: notespace-client
    volumes:
      - ./client:/client
      - /client/node_modules
    restart: always
  api-server:
    build:
      context: ./server/api-server
    container_name: notespace-api-server
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/mydb
      SPRING_DATASOURCE_USERNAME: sh
      SPRING_DATASOURCE_PASSWORD: 1234
    ports:
      - "8088:5000" # swagger
    depends_on:
      - db
    restart: always
  bot-server:
    build:
      context: ./server/bot-server
    container_name: notespace-bot-server
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/mydb
      SPRING_DATASOURCE_USERNAME: sh
      SPRING_DATASOURCE_PASSWORD: 1234
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    depends_on:
      - api-server
      - db
    restart: always
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: sh
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: mydb
    ports:
      - "5433:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
      - ./postgreSQL/init.sql:/docker-entrypoint-initdb.d/init.sql
  nginx:
    image: nginx:latest
    container_name: notespace-nginx
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8081:80"
      - "80:80"
    depends_on:
      - client
      - api-server
      - bot-server
    restart: always
